-----------------------------------------------
-- RELATED WITH SCHEMAS
-----------------------------------------------

-- To list schemas names
SELECT n.nspname
FROM pg_namespace n
ORDER BY n.nspname;
